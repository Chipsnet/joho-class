screen 0, 500, 500

// 最初は一番左にいるので、x座標（横の座標）はゼロ
x = 0

// 今回はyも動くので、y座標（縦の座標）も変数にしてあげる
y = 100

ydown = 10.0

// ずっと繰り返しますよという呪文
while( 1 )

	// さっき指定したxとyを指定
	pos x,y

	// 文字を召喚
	print "*"

	// yの減りを変数にして、1フレームごとにydown分y座標が下になる
	y = y + ydown

	// x座標は特に速度の変化なく一定に増えていく
	x = x + 20

	// ydown（y座標の減少量）を1フレームごとに1.05倍する（これのおかげで放物線に見える）
	ydown = ydown * 1.05

	// これがないとプログラムが止まる（応答なしになる）
	wait 1
// 繰り返す場所はここまでですよという呪文
wend